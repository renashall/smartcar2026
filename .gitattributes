# Keep shell scripts and Python LF so they run cleanly on the Raspberry Pi
# (Linux). Without this, git on Windows can check them out with CRLF, which
# breaks shell shebangs ("/usr/bin/env: 'bash\r'...") and keeps the diffs noisy.
*.sh text eol=lf
*.py text eol=lf
